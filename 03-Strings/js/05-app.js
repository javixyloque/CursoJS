const producto = 'Monitor de 20 pulgadas'

console.log(producto)

//.replace para Reemplazar
console.log(producto.replace('pulgadas' , 'centímetros'))

//.slice para cortar
console.log(producto.slice(2, 14))

//.substring para cortar también pero más inteligente
console.log(producto.substring(4,0))