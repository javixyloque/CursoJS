const numbers = [10, 20, 30];

const months = new Array ('January', 'February', 'March', 'April')


console.log(numbers)
console.log(months)

// Arreglo que tiene datos diversos de todo tipo

const mix = ['Hola', 10, 'si', true, null, { nombre: 'Javi', trabajo: 'Programador esperemos' }]

// Se pueden meter arreglos dentro de arreglos, como con los objetos

console.log (mix)